import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
    marginTop: 48,
    display: "flex",
    justifyContent: 'space-around',
    flexDirection: "row",
    },   
})


